package com.wireshield.enums;

/**
 * The connectionStates enum represents the possible states of a connection.
 */
public enum connectionStates {
    CONNECTED,
    CONNECTION_IN_PROGRESS,
    DISCONNECTED,
}
